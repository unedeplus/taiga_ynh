# Sample nginx configuration for Taiga (YunoHost app)
# This is a template. Use YunoHost helpers to generate the final config.

server {
    listen 80;
    server_name _;
    root /var/www/taiga;
    # ... more config as needed ...
}
